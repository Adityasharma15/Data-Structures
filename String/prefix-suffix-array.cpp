#include<bits/stdc++.h>
#define ll long long
using namespace std;

int main()
{
	ios_base::sync_with_stdio(0);
  cin.tie(0);
	cout.tie(0);

	ll t;
	cin >> t;

	while(t--)
  {

    string s;
    cin >> s;

    ll i = 0, j =1 ;

    ll len = s.length();

    vector<ll>(len , 0)

        if(s[i] == s[j])
          {
            vec[i+1] = vec[i]++;
            i++;
            j++;
          }

          else
          {
            while(  j>=0 && vec[i]!=vec[j])
            {
                j = vec[vec[j]];

            }
          }

    }


	}

  return 0;

}
